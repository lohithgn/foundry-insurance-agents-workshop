# Normalize text files to LF in the repo, native on checkout
* text=auto

# Treat binary assets as binary so Git never applies line-ending conversion
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
